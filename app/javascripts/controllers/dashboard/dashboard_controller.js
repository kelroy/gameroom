//= require "../view_controller"

var DashboardController = new JS.Class(ViewController, {
  
  initialize: function(view) {
    this.callSuper();
    
    this.reset();
  },
  
  reset: function() {
    
  }
});