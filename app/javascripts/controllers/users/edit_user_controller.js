//= require "../../sectionable"
//= require "../view_controller"

var EditUserController = new JS.Class(ViewController, {
  include: Sectionable,
  
  initialize: function(view) {
    this.callSuper();
  },
  
  reset: function() {
  }
});