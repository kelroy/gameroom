var Email = new JS.Class({
  
  initialize: function(params) {
    this.id = params.id;
    this.address = params.address;
  },

  save: function() {
    
  },
  
  valid: function() {
    return true;
  }
});